name: pokrov_platform_contracts
description: Beta platform contracts for the POKROV client program.
publish_to: none
version: 0.2.0-beta.1

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  pokrov_core_domain:
    path: ../core_domain
