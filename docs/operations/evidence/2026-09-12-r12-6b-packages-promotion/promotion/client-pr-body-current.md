Core setup logged caller directories and a listen address even with debug disabled. Bind Android/Windows to Core 6b271de, which writes fixed status messages and the selected mode. Update native hashes, corrected source SBOMs and notices together. Retain the old failure, current checks, and prior source/Windows follow-up without relabeling earlier packages.

Validation: the new regression fails on old setup and passes after the fix; both focused privacy tests and the full Core script pass. Two native DE builds per platform are byte-identical. Four Android ABIs, unchanged Java wrappers, Windows ABI2/event1/15 exports and 100 exact-DLL Dart proxy cycles pass. Six synthetic marker categories are absent from FFI/stdout/stderr and five generated files, including a setup-directory marker. Host route/DNS hashes are unchanged. Consumer analyze, 81 runtime tests, 8 Android tests and seed/docs contracts pass. Five binary scans retain nine prior advisory IDs with bounded dispositions; 191 verbatim notice bodies are unchanged.

The exact Core source is retained on codex/setup-privacy-exact-source-20260912. Its signed promotion tree is identical. PR120 checks out exact Core6b271de; other PR/push checks still use main. The initial run against old Core main failed and is retained; the coordinated source passes local strict seed validation. Existing branch protections are preserved. Current packages, installed TUN/privacy acceptance, full licensing/corresponding-source obligations and public release remain open.
