from pathlib import Path
import subprocess,json,shutil,time
r=Path('/tmp/pokrov-r12-l04-clean-20260911');pid=int((r/'qemu.pid').read_text());assert str(r/'guest.qcow2').encode() in Path('/proc',str(pid),'cmdline').read_bytes()
p=subprocess.run(['ssh','-o','BatchMode=yes','-o','StrictHostKeyChecking=yes','-o','UserKnownHostsFile='+str(r/'guest-known-hosts'),'-i',str(r/'guest-key'),'-p','22265','pokrovqa@127.0.0.1','sudo -n python3 -'],input="from pathlib import Path\nimport subprocess,json,hashlib,time,stat,os,io,tarfile\nr=Path('/home/pokrovqa/acceptance-inputs');p=r/'pokrov_1.2.0~beta.30-8_amd64.deb';count=0\nassert hashlib.sha256(p.read_bytes()).hexdigest()=='b71c3190ad56e534bcd0057b68de4f818e775bc89cf1d5470814468ad7ada933'\nwith tarfile.open(fileobj=io.BytesIO(subprocess.check_output(['dpkg-deb','--fsys-tarfile',str(p)]))) as tar:\n for e in tar:\n  if not e.isfile() and not e.issym():continue\n  p=Path('/')/e.name.removeprefix('./');st=p.lstat();assert st.st_uid==st.st_gid==0\n  if e.issym():assert p.is_symlink() and os.readlink(p)==e.linkname\n  else:assert not p.is_symlink() and hashlib.sha256(p.read_bytes()).digest()==hashlib.sha256(tar.extractfile(e).read()).digest() and stat.S_IMODE(st.st_mode)==e.mode\n  count+=1\nrows=[]\nfor text in subprocess.run(['pgrep','-x','pokrov'],capture_output=True,text=True).stdout.split():\n p=Path('/proc',text);assert (p/'exe').resolve()==Path('/usr/lib/pokrov/ui/pokrov');rows.append({'pid':int(text),'uid':p.stat().st_uid})\nassert len(rows)==1 and rows[0]['uid']==1000\nsock=Path('/run/pokrov/pokrov-linuxd.sock');st=sock.stat()\nj=subprocess.run(['journalctl','-u','pokrov-l04-modes-20260913','--since','@1789286100','--no-pager','-o','json'],capture_output=True,text=True)\nmessages=[json.loads(x).get('MESSAGE','') for x in j.stdout.splitlines()] if j.returncode==0 else []\nv={'epoch':time.time(),'checked_payload_entries':count,'gui':rows,'socket':{'mode':oct(stat.S_IMODE(st.st_mode)),'uid':st.st_uid,'gid':st.st_gid,'is_socket':stat.S_ISSOCK(st.st_mode)},'no_tun':not Path('/sys/class/net/pokrov0').exists(),'no_recovery':not Path('/var/lib/pokrov/network-recovery.json').exists(),'modes_unit_deactivated_successfully':any('Deactivated successfully' in m or 'Succeeded.' in m for m in messages),'modes_unit_failure_message_present':any('Failed with result' in m for m in messages),'native_scripts':{name:hashlib.sha256((r/name).read_bytes()).hexdigest() for name in ['guest-de-recheck-20260913.py','guest-modes-20260913.py']}}\nprint(json.dumps(v))\n",capture_output=True,text=True,timeout=25);assert p.returncode==0,p.stderr;v=json.loads(p.stdout);v.update(mini_qemu_pid=pid,mini_free_bytes=shutil.disk_usage(r).free);print(json.dumps(v))
