# Materialized package evidence

The 52 files in the original archive are retained here byte for byte, verified
against the `archive.inventory` in [the original receipt](../receipt.json).
The original ZIP remains unchanged locally at
`E:/r12-c05-utls-license-20260911/package-evidence.zip` and in its original Git
history. ZIP files are excluded from current client source by the existing
repository hygiene contract; no validation rule was weakened.

The first validation after the prior evidence commit detected this packaging
error. Earlier pre-staging checks did not prove the final tracked ZIP was
accepted. This materialization preserves all original captures and failures.
