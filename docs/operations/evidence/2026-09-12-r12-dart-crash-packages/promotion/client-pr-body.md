The shared crash handlers saved a sanitized marker and then forwarded the original error and stack to standard handlers. Flutter's pinned release fallback prints those values. Stop forwarding them and mark isolate errors handled, keeping the existing closed synchronous crash marker.

Validation: a real Flutter-engine regression reproduces synthetic password/URL disclosure through the original default console handler. With the fix, both Flutter and isolate callbacks retain CRASH-001 without console/prior-handler forwarding; eight focused tests, package analysis, docs contract and explicit-root seed validation pass. The first failed test used Flutter's test dispatcher, whose onError setter is a no-op in this pinned SDK; that harness failure is separately retained, and the final regression uses the real dispatcher. The release logging branch was inspected in pinned SDK source; this does not claim an installed release APK executed the planted Dart exception. Android/Windows successor packages and installed checks remain pending. No Core/backend deployment or public release is included.
