# Android startup, numeric summary and support consent — 2026-09-12

Document class: EVIDENCE. [Exact receipt and captures](receipt.json).

The owned LDPlayer14 index3 upgraded in place to client `f0711b69`, Core
`6b271dec`, x86_64 APK `6d4b9987…`. Data was not cleared. The actual main screen
loaded, and the available app-PID logcat contained no unhandled/FATAL exception.
This proves recovery from the preceding `a9bc280` flavor startup failure for
these installed bytes. Client PR122 and its exact main commit CI passed; branch
protection before/after is identical. This is source promotion, not a release.

The explicit app Create case with bundle action created owned case59. Server
validation received all 2398 encrypted bytes. Its admin build number is `4053`;
in-memory worker decryption confirms `build/identity.json` contains build_id
`4053`, channel `direct`, version `1.2.0`, platform `android`. The three-file
summary plaintext was 1430 bytes. Exact HTTPS lookup, step-up denial, single-use
no-store download, replay denial and two audit records passed. The fixture
operator session was revoked with earlier sessions unchanged. Case59 remains
open. This summary did not contain extended/crash files or a new planted-input
native-to-app test; those remain separate requirements.

## Native consent failure and narrow source fix

A normal confirmed L2 action issued a 15-minute policy for the owned case58,
Android build4053, five categories and at most two 1MiB bundles/2MiB total. The
code remained in the existing root-only remote private file and in memory during
input. The app redeemed it and displayed the actual disclosure. On this
1080x1920 / 480dpi fixture the bottom sheet did not fit: the Enable button was
partly beyond its layout, repeated ordinary taps did not enable it, and there
was no scrollable node. Screenshot and semantic tree are retained. Android Back
closed the consent; the app continued to show the mode off. No extended bundle
was sent, and the redeemed policy was not reused.

The fix only allows this consent sheet to use the available height and changes
its existing padded content into a scroll view. Disclosure, explicit consent
and policy validation stay intact. The 18 existing diagnostics/controller tests
and package analysis pass; they are regression checks, not native proof of the
new consent layout. Rebuilt installed confirmation remains pending.

## Fixture recovery

The initial guard found index3 renamed to LDPlayer-3 with default display
settings; its config hash exactly matched the preceding root-restoration
receipt. The previously observed name/resolution were restored while preserving
device property fields and the actual vbox four CPUs/4096MiB. Other instances
were unchanged. ADB was absent in that config and was enabled for this fixture.
Two rooted attempts could not obtain a ready ADB session; neither installed the
new APK nor injected a profile. After both exact fixture processes stopped,
root was set false with every other current field preserved. This boot succeeded
and guest su returned127 before the app checks above. Thus the earlier claim was
only config-level root restoration; this capture adds the actual guest check.

All three boots ended stopped with sampled C/E free above40GiB and disk growth
within512MiB. Other instance disks/configs and host route/DNS hashes were
unchanged. Hiddify and the physical phone were not touched. No source profile,
credential, activation code or decrypted content was exported into these files.
