<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Android 12+ branded splash: pokrov-clear light canvas (#F5F7F6)
         behind the adaptive launcher mark. The icon reuses
         @mipmap/ic_launcher so the splash and launcher stay one brand
         surface without generating new artwork. -->
    <style name="LaunchTheme" parent="@android:style/Theme.Light.NoTitleBar">
        <item name="android:windowSplashScreenBackground">#F5F7F6</item>
        <item name="android:windowSplashScreenAnimatedIcon">@mipmap/ic_launcher</item>
        <item name="android:windowBackground">@drawable/launch_background</item>
    </style>
    <style name="NormalTheme" parent="@android:style/Theme.Light.NoTitleBar">
        <item name="android:windowBackground">?android:colorBackground</item>
    </style>
</resources>
